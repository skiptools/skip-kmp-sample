# Configuration file for https://skip.tools project
#
# Kotlin dependencies and Gradle build options for this module can be configured here

settings:
  contents:
    - block: 'sourceControl'
      contents:
        - block: 'gitRepository(java.net.URI.create("https://github.com/marcprux/kmp-library-sample.git"))'
          contents:
            - 'producesModule("kmp-library-sample:multi-platform-library")'

build:
  contents:
    - block: 'dependencies'
      contents:
        - 'implementation("kmp-library-sample:multi-platform-library:0.1.3")'
        #- 'implementation("kmp-library-sample:multi-platform-library") { version { branch = "main" } }'


